# Ontology Bundle Manifest

- bundle_id: `v3_second_pass_candidate`
- purpose: targeted ontology and prompt companion bundle for the post-`v2` cleanup pass
- source: copied from `v2_second_pass_candidate` before applying `v3` edits
- intended use: targeted rerun on the remaining hard-organ failure modes after `v2`

This bundle is the editable `v3` working ontology.
